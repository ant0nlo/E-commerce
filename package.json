{
  "name": "my-ecommerce-app",
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "build:frontend": "cd frontend && npm start",
    "install:backend": "cd backend && npm install",
    "install:services": "cd backend/order-service && npm install && cd ../payment-service && npm install && cd ../shipment-service && npm install && cd ../notification-service && npm install",
    "build": "npm run install:frontend && npm run build:frontend && npm run install:backend && npm run install:services",
    "start:backend": "cd backend && npm start"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
